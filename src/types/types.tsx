export interface IOptions {
    value: number | string;
    name: string;
}

export interface IPosts {
    id: number,
    title: string,
    body: string;
}

/*
export interface IPost {
    id: number,
    title: string,
    body: string;
}*/
